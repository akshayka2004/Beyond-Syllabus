Home-page artwork slots.

Save the real illustrations here (transparent PNG works best). Until a file
exists, the home page automatically shows a hand-coded SVG fallback, so nothing
breaks if a file is missing.

Expected files:
  bridge.png    Hero — person crossing a plank from "OUTDATED SYSTEM" to
                "REAL WORLD POSSIBILITIES" (a hand drawing the bridge).
  pledge.png    Crowd holding the "OUR VOICES. OUR FUTURE." banner
                ("One pledge can change everything.").
  sunrise.png   People, arms over shoulders, watching the sunrise over mountains.
  mascot.png    The mascot character (kept in full colour — not inverted in dark mode).

Notes:
- bridge / pledge / sunrise are pencil sketches; they are auto-inverted in dark
  mode (filter: invert + hue-rotate) so they read as white-on-dark.
- mascot.png is NOT inverted (it stays its real colour in dark mode).
- If you also have a separate wordmark LOGO image (not the mascot), save it as
  logo.png here and tell me — I'll wire it into the header/footer.
